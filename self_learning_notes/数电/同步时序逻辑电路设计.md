一般过程：
一、逻辑抽象
1确定变量：输入、输出
2明确含义：对应各状态下变量情况
3、列状态表
举例：
```
\documentclass{article}
\usepackage{array}

\begin{document}

\renewcommand{\arraystretch}{1.5}
\begin{center}
    \begin{tabular}{|c|c|c|c|}
        \hline
        当前状态 & 输入 X=0 & 输入 X=1 & 输出 Y \\ 
        \hline
        S0 & S0 & S1 & 0 \\ 
        \hline
        S1 & S2 & S1 & 0 \\ 
        \hline
        S2 & S0 & S3 & 0 \\ 
        \hline
        S3 & S0 & S4 & 0 \\ 
        \hline
        S4 & S0 & S0 & 1 \\ 
        \hline
    \end{tabular}
\end{center}

\end{document}
```

二、状态化简和分配
1、状态合并（直接读表 
2、确定数量：n台触发器有$2^n$种状态（为什么?），状态数M<= $2^n$
3、编代码：状态编码原则 (格雷码比较简便) 

三、选触发器类型
1、选器件
2、写方程：状态方程、输出方程、驱动方程
状态方程和驱动方程都可以直接读
奇技淫巧：从卡诺图上按照触发器类型划分区域 分别在每个区域圈出1 然后直接得到驱动方程（这个后面仔细看一下）

四、画图
1、画电路
2、查自启（怎么处理自启 无关项化简：卡诺图