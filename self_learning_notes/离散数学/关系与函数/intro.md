关系：
eg 笛卡尔积：A × B 
    SELECT x , y FROM A , B

二元关系：
	A，B是集合，从 A 到 B 的一个 关系 R 是笛卡尔积 A × B 的一个子集
	 若 A == B，称上述关系为集合上的二元关系

n元构成$2^{n^2}$ 组互相不同的关系 
解释 n元集合的笛卡尔积有 $n^2$ 个元素，也就是说一个关系最多有这些元素
每个小一些的关系可以从中任意挑选一些元素对来构造，也就是说，对于每个元素对，有取/不取两种选择
所以一共有 $2^{n^2}$ 这么多的组合方式
