FOUR=[1,2,3,4]
#FOUR 是引用，类似于指针，指向列表的第一个元素，因此修改FOUR会修改列表的第一个元素
'''引用的定义：变量名指向一个内存地址，指向的内存地址中存放的是一个值。
非引用变量在赋值时，会将原变量的值复制一份到新变量中，因此修改非引用变量不会影响原变量的值。
（eg：把变量当参数传递给函数时，传递的是变量的副本，修改副本不会影响原变量的值）

可以直接被修改（可变）的数据类型：list，dictionary
不能直接被修改的数据类型：int，float，string，tuple，bool
元组内部的列表可以改变（因为列表其实存储是箭头（地址），地址在元组里是没有变的，变的是箭头指向的地址里的值）'''
s=[1,6]
FOUR.append(s)#four后面加一个存箭头的空间
FOUR.exdend(s)#four后面拼接一个s，这个s是复制的
#  + 操作符是把左右都复制一份
[a]+[b]->[a,b]



